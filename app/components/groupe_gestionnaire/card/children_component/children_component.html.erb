<%= render SettingsTileComponent.new(
  url: @path,
  badge: { label: t(".badge.validated"), variant: :success },
  counter: @groupe_gestionnaire.children.size,
  link_attributes: { id: "children" }
) do |tile| %>
  <% tile.with_title_content(t(".title", count: @groupe_gestionnaire.children.size)) %>
<% end %>
