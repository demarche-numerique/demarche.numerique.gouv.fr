<div class="fr-fieldset__element">
  <%= @form.label :department_code, for: focusable_departement_input_id, class: 'fr-label', id: input_label_id(@champ, :department_code) do %>
    <%= t(".departement_label") %>
  <% end %>

  <%= @form.select :department_code, departement_options, departement_select_options, required: @champ.required?, id: focusable_departement_input_id, class: "width-33-desktop width-100-mobile fr-select", aria: labelledby_id_attr(input_label_id(@champ, :department_code)) %>
</div>

<% if @champ.departement? %>
  <div class="fr-fieldset__element">
    <div class="fr-select-group">
      <%= @form.label :value, for: focusable_epci_input_id, class: 'fr-label', id: input_label_id(@champ, :value) do %>
        <%= t(".epci_label") %>
      <% end %>

      <%= @form.select :value, epci_options, epci_select_options, required: @champ.required?, id: focusable_epci_input_id, class: "width-33-desktop width-100-mobile fr-select", aria: labelledby_id_attr(input_label_id(@champ, :value)) %>
    </div>
  </div>
<% end %>
