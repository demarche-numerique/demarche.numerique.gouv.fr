<%= render SettingsTileComponent.new(
  url: annotations_admin_procedure_path(@procedure),
  badge:,
  counter: @count,
  subtitle: "Champs à remplir par l’administration",
  link_attributes: { title: error_messages }
) do |tile| %>
  <% tile.with_title_content(t(".title")) %>
<% end %>
