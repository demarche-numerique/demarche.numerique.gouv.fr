<%= render SettingsTileComponent.new(
  url: admin_procedure_email_templates_path(@procedure),
  badge:,
  counter: customized_progress,
  subtitle: "Notifications automatiques"
) do |tile| %>
  <% tile.with_title_content(t(".title")) %>
<% end %>
