<%= render SettingsTileComponent.new(
  url: edit_admin_procedure_dossier_submitted_message_path(@procedure),
  badge:,
  subtitle: t(".sub_title")
) do |tile| %>
  <% tile.with_title_content(t("shared.dossier_submitted_message.title")) %>
<% end %>
