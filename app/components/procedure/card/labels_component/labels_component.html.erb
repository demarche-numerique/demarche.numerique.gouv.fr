<%= render SettingsTileComponent.new(
  heading_level: :h4,
  url: [:admin, @procedure, :labels],
  badge:,
  counter: @procedure.labels.presence&.size,
  subtitle: t(".subtitle")
) do |tile| %>
  <% tile.with_title_content(t(".title")) %>
<% end %>
