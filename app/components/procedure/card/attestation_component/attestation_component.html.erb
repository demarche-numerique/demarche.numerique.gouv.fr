<%= render SettingsTileComponent.new(
  heading_level: :h4,
  url: edit_attestation_path,
  badge:,
  subtitle: t(".#{@kind}.subtitle")
) do |tile| %>
  <% tile.with_title_content(t(".#{@kind}.title")) %>
<% end %>
