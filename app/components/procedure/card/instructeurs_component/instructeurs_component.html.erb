<%= render SettingsTileComponent.new(
  url: admin_procedure_groupe_instructeurs_path(@procedure),
  badge:,
  counter: count,
  subtitle: t(".subtitle"),
  link_attributes: { id: "groupe-instructeurs" }
) do |tile| %>
  <% tile.with_title_content(title) %>
<% end %>
